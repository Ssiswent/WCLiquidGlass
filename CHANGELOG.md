# 更新日志

## [1.6.75] - 2026-07-28

### 修复

- 修复微信冷启动后立即切换到通讯录时，插件与微信首次数据加载竞争、导致页面约 3 秒黑屏的问题。
- 通讯录首次识别时不再强制执行整表 `reloadData`；仅在用户后续修改圆角或液态配置时刷新已初始化的通讯录。
- 保持已经确认的主页连续会话 section、通讯录液态 section、圆角、间距、内容布局及头像兼容处理不变。

## [1.6.74] - 2026-07-28

### 修复

- 通讯录头像改为由稳定的 `MMHeadImageView` 外层容器统一执行圆形裁切，不再读写头像插件管理的内部图像层，彻底避免初始化、滚动复用和页面切换时内部圆角被异步重置后随机变方。
- 保持通讯录现有的液态 section、圆角、内外边距、内容位置和点击区域不变。

## [1.6.72] - 2026-07-28

### 修复

- 修正通讯录头像兼容逻辑的圆角来源：从同一头像容器内由其他插件处理过的同级图像层继承圆角，避免初始化、滚动复用或页面切换后实际显示层随机恢复为方形。
- 继续保持通讯录 section 的液态效果、圆角、间距、内容布局和点击区域不变。

## [1.6.71] - 2026-07-28

### 修复

- 修复开启“卡片化主页列表”后，通讯录头像外层容器在 Cell 复用布局时偶发丢失其他插件圆角效果的问题。
- 仅继承头像内部图像层已经存在的圆角状态，不指定统一圆角、不改头像尺寸，也不影响通讯录现有的液态 section、间距和点击区域。

## [1.6.70] - 2026-07-28

### 修复通讯录首个合并 Section 左侧内边距

- 根据真机层级日志，改为对首个合并 Section 的真实内容容器应用 16pt 左侧内边距，避免被 `NewContactsItemCell` 的原生布局重置。
- 保持其他 Section、上下内边距、圆角、玻璃背景和 Cell 点击区域不变。
- 保存并恢复原始内容容器 frame，避免滚动和 Cell 复用时发生内边距累加或位置漂移。

## [1.6.68] - 2026-07-28

### 通讯录左侧内边距加宽

- 在 1.6.67 稳定且不累加的 frame 布局基础上，将通讯录内容左侧内边距从 8pt 增加到 16pt，右侧继续保持 8pt。
- 不改变上下内边距、section 圆角和背景、Cell frame 或整行点击区域。

## [1.6.67] - 2026-07-28

### 通讯录内边距布局根治

- 移除会改变微信内部坐标系、并在滚动和 Cell 复用时产生反馈偏移的 `contentView.bounds` 方案，解决通讯录左侧内容位置随滚动、切页和复用乱跑的问题。
- 改为在微信完成原生布局后，仅调整 `contentView` 的外部 frame：左右各保留固定 8pt 内容内边距，不改变内部子视图坐标、Cell frame 或整行点击区域。
- 记录每个 Cell 的原生 frame 与已应用 frame，重复布局时始终从原生几何重新计算，不累加位移；保留现有上下内边距、section 合并、右侧 A–Z 索引避让及主页定型效果。

## [1.6.66] - 2026-07-28

### 通讯录左侧内边距稳定化

- 修正上一版只右移 8pt 时，头像装饰层只能与 Card 左边缘齐平、无法形成真实内边距的问题；通讯录内容改为相对原生位置稳定右移 16pt，使最外层装饰与 Card 之间保留约 8pt。
- 不再使用会被微信 Cell 复用和布局过程反复重置的 `contentView.transform`，改用不累积的 `contentView.bounds` 坐标偏移。
- 在 `UITableView` 完成整轮原生布局后重新确认统一偏移，避免滚动、复用和页面切换导致左侧间距忽大忽小；Cell frame 和整行点击区域保持原生状态。

## [1.6.65] - 2026-07-28

### 通讯录内外边距分离

- 通讯录在微信原生 section header 布局阶段为每个视觉分组增加 8pt 顶部空间，再由液态背景向上包裹该空间；Card 与搜索栏、分组标题的外部距离保持不变，同时恢复顶部 8pt 内边距。
- 通讯录 Cell 保持原生 frame 与整行点击区域不变，仅将 contentView 及其内部命中区域整体右移 8pt，使头像、图标和文字收进 Card 左侧边缘。
- Card 继续保持左 16pt、右 24pt 外边距，连续 section 合并和主页定型实现不变。

## [1.6.63] - 2026-07-28

### 通讯录分组外边距微调

- 通讯录液态 Card 顶部恢复到微信原生 section 起点，不再向上扩展 8pt，重新保留搜索栏与首组、“我的企业”等分组标题与下方 Card 的原生间距。
- 通讯录 Card 左侧固定保留 16pt 屏幕外边距，右侧继续保留 24pt，兼顾左右视觉平衡与 A–Z 快捷索引栏空间。
- 底部继续保留 8pt 内容包裹空间；连续 section 合并方式、原生 Cell 和点击区域保持不变。

## [1.6.62] - 2026-07-28

### 通讯录连续分组与安全内边距

- 通讯录不再按微信的原生 section 编号拆分玻璃背景，而是将几何上连续的 section 合并为一个液态容器；“新的朋友”到“企业微信联系人”保持为一张连续 Card，遇到“我的企业”等真实 header 间隔才开始新 Card。
- 所有通讯录 section 与 Card 的液态背景在内容上下左右额外扩展 8pt，不改变原生 Cell、内容布局和点击区域。
- 通讯录液态背景右侧固定预留 24pt，避免遮挡 A–Z 快捷索引栏；主页、发现页和“我”页保持不变。

## [1.6.61] - 2026-07-28

### 通讯录分组间距与内边距

- 通讯录各原生 section 的液态背景上下各收进 4pt，使“新朋友～标签”、公众号、服务号和企业微信联系人等相邻分组之间形成固定 8pt 视觉间距。
- 通讯录 section 背景左右扩展至 8pt 页面缩进，为头像、图标和文字保留额外的背景内边距，避免内容贴住或越过液态卡片边缘。
- 仅调整通讯录玻璃背景的可视 frame，不移动原生 Cell、内容和点击区域；主页已定型的连续会话 section 及其他页面保持不变。

## [1.6.60] - 2026-07-28

### 主页连续玻璃修复

- 根据真机层级日志确认主页 section 玻璃已正确创建，但微信随后把 `NewMainFrameCell` 背景重写为完全不透明的深色或浅色底，遮住了后方的液态材质与整体圆角。
- 仅在主页会话 Cell 类拦截这次背景回写并保持透明，让 section 级 `UIGlassEffect` 正常显示；不修改已经正确的发现、通讯录和我页面。
- 保持原生会话内容、点击、滑动和复用流程不变，避免重新引入全局 Cell Hook 的启动风险。

## [1.6.59] - 2026-07-28

### 连续分组液态卡片

- 连续模式不再为每个 Cell 分别创建玻璃层，改为每个微信原生 section 只创建一个完整的系统 `UIGlassEffect` 容器，首尾圆角由整个分组统一裁切。
- 主页会话、通讯录和我页面的连续分组移除逐行材质边缘与原生分割线，避免中间项目出现独立圆角、亮线和断层。
- 连续分组内的 Cell 保持透明并仅承载原生内容与触摸交互，去掉主页深色模式灰黑底、浅色模式白底的重复叠加；主页“每条独立圆角卡片”开关继续保留。

## [1.6.58] - 2026-07-28

### 纯液态材质与连续卡片

- 恢复主页“每条独立圆角卡片”开关：开启时使用独立卡片和可调会话间距，关闭时按微信原生分组生成首行上圆角、中间直角、末行下圆角的连续液态卡片。
- 发现、通讯录和我页面固定保留微信原生连续分组，不再修改原生 Cell 高度、会话间距、图标或文字位置，避免布局与点击区域错位。
- 删除 Cell 半透明底色、非主页稳定底色和额外玻璃 tint 三层重复合成；所有页面只保留系统 `UIGlassEffect`，通透与色调完全跟随插件“液态效果”设置。
- 删除首次展示后的延迟重绘，避免滚动或页面返回时在通透材质与灰白材质之间切换。

## [1.6.57] - 2026-07-28

### 其它主页面玻璃一致性

- 修复发现、通讯录和我页面首次打开与滚动后使用两套卡片合成路径的问题：在原生可见 Cell 完成创建后立即补齐稳定玻璃层，不再等第一次滚动才应用液态背景。
- 三个页面现在统一使用稳定的 26 pt 玻璃底层，避免朋友圈因复用或返回而在初始通透状态与灰白玻璃状态之间反复切换。

## [1.6.56] - 2026-07-28

### 发现页稳定性与诊断清理

- 移除首页圆角设置页中的“启动兼容诊断”分组，并删除对应的偏好项、启动标记文件和三条已停用的全局/多菜单 Hook 代码；完整崩溃采集与手动页面层级诊断继续保留。
- 发现页卡片使用独立且更稳定的玻璃底层与上层色彩合成，不再依赖朋友圈返回时微信短暂恢复的亮色原生底图，降低返回瞬间背景突然变亮的闪烁。

## [1.6.55] - 2026-07-28

### 发现页卡片细节

- 发现页液态卡片的图标与标题作为一个整体在卡片内水平、垂直居中，并隐藏右侧箭头；保留原生 Cell 的点击区域和朋友圈转场。
- 为发现页卡片增加稳定的半透明玻璃底层，覆盖微信从朋友圈返回时短暂重建的原生背景，避免在更透明与灰白玻璃状态之间闪烁。

## [1.6.54] - 2026-07-28

### 发现页转场修复

- 朋友圈等发现页项目改回保留微信原生 Cell 几何：不再在列表布局期间重设真实 Cell frame，而是只在 Cell 内铺设与卡片一致的液态背景层。
- 避免朋友圈转场同时持有原生源视图快照与被重设几何的 Cell，修复点击进入、返回后延迟出现的多层放大和重影；发现页的 26 pt 圆角与 8 pt 间距保持不变。

## [1.6.53] - 2026-07-28

### 紧急启动恢复

- 发现第 3 项除多菜单高亮外，还会安装全局 `UITableViewCell` 高亮 Hook；第 1 项则安装全局 frame Hook。为避免已保存的任意组合再次导致启动闪退，本版启动时隔离全部三个“启动兼容诊断”Hook。
- 首页圆角、液态背景、原生行高与布局流程继续保留；仅停止三个用于定位问题的全局/多菜单方法替换，恢复后无需先进入设置关闭开关。

## [1.6.52] - 2026-07-28

### 启动恢复

- 临时隔离“多菜单背景 Hook”和“多菜单高亮 Hook”的注入路径；即使 1.6.51 中这两项曾被开启，微信启动时也不会再安装对应 Hook，优先恢复可启动性。
- 保留首页圆角、液态背景、间距与其余布局代码，不回滚此前功能；启动诊断标记会额外记录多菜单 Hook 已隔离，便于后续根据可复现证据重新实现。

## [1.6.51] - 2026-07-28

### 启动兼容诊断

- 保留 1.6.50 的圆角、液态背景和完整崩溃采集实现，新增“启动兼容诊断”分组，用于精确排查启动期闪退。
- 将本版新增的全局布局帧 Hook、多菜单背景 Hook、多菜单高亮 Hook 拆为三个独立开关；诊断版默认均关闭，每次只开启一项并重启微信即可进行对照测试。
- 每次安装首页圆角 Hook 时，都会将当前阶段和三个开关状态持久化写入 `Startup-Hook-Diagnostics.txt`，即使微信随后退出也可用于确认启动流程走到哪里。

## [1.6.50] - 2026-07-28

### 修复与优化

- 首页圆角的卡片几何改在微信原生 `setFrame:` 布局阶段完成，缩进、圆角、内容布局和触摸命中共用同一套坐标，修复设置后点击区域可能错位的问题。
- “我”页卡片不再通过扩展背景高度制造间距；图标、文字与箭头重新由原生布局居中，避免视觉上整体偏下。
- 顶部“Mac 微信已登录”行固定为 26 pt 液态玻璃卡片，并清理内部不透明按钮容器，切换页面或滚动后不再在直角、白底和玻璃状态间跳变。
- 拦截发现页列表复用时的原生背景回写，并禁用朋友圈卡片高亮放大反馈，减少滚动闪烁、点击重影与返回时的放大延迟。
- 新增《德发首页圆角》实现分析文档，沉淀原生布局、连续卡片首中尾圆角、背景稳定与内容居中方案，供后续优化复用。

## [1.6.41] - 2026-07-27

### 修复与优化

- 首页圆角的间距改为由微信列表原生行高计算提供，不再在布局完成后移动或压缩 Cell；视觉留白、滚动范围与点击命中使用同一套坐标。
- 主页会话内容层随新增行高居中，首个会话与分组标题、相邻会话之间均保留对称间距，不再出现内容贴边或被压缩。
- 液态卡片改为独立的材质层与色彩底层，避免微信重置 Cell 背景后出现白底或灰底；发现、通讯录和我继续固定为 26 pt 圆角并共用间距配置。

## [1.6.40] - 2026-07-27

### 修复与优化

- 首页及其它三个主页面的间距改为仅作用于玻璃背景层，上下各占一半；分组标签与首项、相邻卡片之间的视觉留白保持一致。
- 不再移动任何原生 Cell 或扩展列表内容高度，修复主页与通讯录在开启间距后点击位置错位的问题。
- 发现、通讯录和我页面的圆角固定为 26 pt，不再跟随主页圆角调节。

## [1.6.39] - 2026-07-27

### 修复与优化

- 会话间距改为按分组内行号累积，首项保持原位，间距仅出现在前一项下方；不再额外拉开分组标题与第一条会话。
- 发现、通讯录和我页面继续统一使用圆角、左右缩进与液态背景，但保持微信原生纵向布局，修复设置会话间距后视觉位置与点击区域错位的问题。

## [1.6.38] - 2026-07-27

### 修复与优化

- 首页圆角的间距改为保持微信原始 Cell 高度、按序移动 Card 并扩展列表可滚动范围；提高间距不再压缩头像和文本内容。
- 聊天、发现、通讯录和我页面现在固定共用一组圆角、左右缩进、会话间距与液态背景配置；移除“同步到发现/通讯录/我”板块及单独圆角设置。
- 为其它三个主 Tab 的表格补充布局更新路径，确保液态背景在其原生 Cell 布局完成后再应用，而不是只留下透明底。

## [1.6.37] - 2026-07-27

### 调整与修复

- 根据真机层级确认，连续卡片的背景与圆角会被同时注入的首页圆角插件再次覆盖；移除“每条独立圆角卡片”开关，首页固定使用已验证稳定的独立液态卡片。
- 独立卡片上下各扩展 2pt 内部空间，使头像与内容的上下留白进一步增加并保持均衡。
- 更新首页圆角设置文案与项目顺序，移除已不再支持的连续显示说明。

## [1.6.36] - 2026-07-27

### 优化

- 连续首页卡片取消每个 Cell 的半透明底色叠加，仅保留内容层液态玻璃；相邻玻璃面板在原生 1pt 重叠处精确收边，移除白色伪分割线，并保持首尾与中间 item 的连续圆角过渡。
- 独立卡片仅上移微信原生会话内容半个间距，使头像外圈在 Card 内的上下留白与裁切更均衡，底部不再贴边。

## [1.6.35] - 2026-07-27

### 回退与修复

- 首页圆角恢复 1.6.28 已在真机验证有效的 Cell 内容层 `UIGlassEffect` 渲染路径；移除连续模式中不兼容微信最终 Cell 绘制的共享表格底层玻璃、延时重绘与背景 setter 拦截。
- 保留页面层级诊断增强，便于后续仅围绕“连续无缝”和“独立卡片留白”分别优化。

## [1.6.34] - 2026-07-27

### 修复

- 连续首页卡片改为在保留微信 `NewMainFrameCell` 私有 setter 调用链的前提下，精确拦截其后续不透明底色写入并保持透明；组级液态玻璃不会再被白色 Cell 底板覆盖。
- 不再绕过私有 Cell 的背景实现，避免重现 1.6.31 的启动阶段闪退风险。

## [1.6.33] - 2026-07-27

### 修复

- 连续卡片在微信及其他插件完成本轮布局约 50ms 后，再安全地应用透明 Cell 与组玻璃状态；避免白色底色覆盖材质，同时不再触碰可能造成启动闪退的私有背景 setter。

## [1.6.32] - 2026-07-27

### 紧急修复

- 移除 1.6.31 中对 `NewMainFrameCell setBackgroundColor:` 的底层 hook。该 hook 会在微信创建会话 Cell 的早期阶段介入私有背景状态管理，可能导致启动即闪退。
- 保留连续卡片的组玻璃层级修复与诊断增强，恢复安全的布局后应用路径。

## [1.6.31] - 2026-07-27

### 修复

- 连续卡片的组级液态玻璃在微信重排表格子视图后会被移到会话 Cell 上方；现在每次更新都会重新置于所有会话 Cell 下方，避免覆盖、变形和白色遮罩。
- 为 `NewMainFrameCell` 增加底色回写保护：微信或其他注入插件在后续布局中写入不透明底色时，连续模式会保持透明，独立模式会恢复对应液态底色。

## [1.6.30] - 2026-07-27

### 修复与诊断

- 修复连续卡片中微信或其他已注入 hook 在布局链末尾重新写入不透明 Cell 底色的问题：当前布局轮次结束后仅补一次最终应用，保证连续组玻璃面板不会被白色 Card 遮挡。
- 独立卡片的内容上移也在最终应用阶段确认，避免微信后续布局覆盖后头像再次贴近卡片底部。
- 页面层级诊断新增 `MainFrameTableView` 的直接子视图列表，若第三方插件改变组材质的父层或叠放顺序，可直接从日志定位。

## [1.6.29] - 2026-07-27

### 修复与优化

- “每条独立圆角卡片”模式中，将会话内容向上微调半个卡片间距，使头像与文字在卡片内的上下视觉留白更均衡。
- 连续卡片模式改为每个会话组共享一张液态玻璃面板：仅首尾保留组圆角，中间 item 透明叠放于同一材质上，消除各条材质边缘造成的断层与白色伪分割线。
- 组级材质只覆盖当前可见区域及缓冲区，随列表滚动更新，避免为长会话列表创建过大的离屏材质表面。

## [1.6.28] - 2026-07-27

### 修复与优化

- 首页圆角的真实会话 Card 保持透明，同时在内容层底部加入 `UIGlassEffect`；避免 `backgroundView` 被微信重置后，仍可呈现液态玻璃折射与背景透出。
- 从表格与会话 Card 的直接 separator 视图两层隐藏分割线，避免微信私有分隔线在系统 `separatorStyle` 关闭后仍然显示。
- 设置页普通 Card 与顶部品牌 Card 固定使用统一的中性外观；切换“液态效果”的透明、平衡、色调档位不再改变设置一级页或普通二级页的背景颜色。

## [1.6.27] - 2026-07-27

### 修复与优化

- 重写首页圆角液态背景渲染：直接将透明动态底色写入微信真实会话 Card，而非在其下方叠加会被遮挡的材质视图；背景图可透出，并跟随“液态效果”档位调整透明度与色调。
- 移除会话 Cell 内部递归扫描、背景视图反复替换及全表滚动布局处理；改为在 `NewMainFrameCell` 完成布局后按缓存更新，显著降低首页上下滑动的额外消耗。
- 保持系统分割线隐藏；相邻会话卡片使用统一透明底色，移除由不透明白灰底色形成的伪分割线。

## [1.6.26] - 2026-07-27

### 修复

- 根据首页真实层级修复液态背景：在 `NewMainFrameCell` 自身布局结束后清除微信重新写入的白灰底色，并将液态材质放入 Cell 背景层，使其采样首页底图而非 Cell 自身颜色。
- 首页会话之间不再出现由相邻 Cell 白灰底色差异造成的伪分割线；系统分割线继续保持隐藏。

## [1.6.25] - 2026-07-27

### 新增

- “按钮与动作”新增“当前页面层级诊断”：在环形菜单完全收起后采集当前页面的控制器、视图、表格与 Cell 布局状态，并写入“崩溃日志”。
- 诊断日志不会记录聊天文字、联系人名称或辅助功能文本，仅保留类名、层级、尺寸、背景、圆角与分割线状态，便于定位首页圆角材质未生效的问题。

### 调整

- 首页圆角设置页改为复用插件统一的二级页面背景、Card、字体与导航样式，避免下滑时导航栏背景颜色异常。

## [1.6.24] - 2026-07-27

### 修复与优化

- 将首页圆角液态材质移入会话内容层底部，避开微信 Cell 的背景层覆盖，确保壁纸与液态折射效果可见。
- 同时清理会话 Cell 内部白色遮罩和横向细线，并在列表布局时持续维持无分割线状态。
- 重做“首页圆角”设置页：采用插件统一的渐变背景、液态顶部 Card、连续圆角设置分组、字体与颜色体系。

## [1.6.23] - 2026-07-27

### 修复

- 移除“首页圆角”中的背景颜色项目，只保留液态背景。
- 修复液态背景被微信会话 Cell 内部白色内容层遮挡的问题：开启后会透出底部壁纸与材质效果，并继续跟随“液态效果”档位。
- 移除首页圆角目标列表内的会话分隔线；关闭首页圆角后会恢复微信原本分隔线与背景样式。

## [1.6.22] - 2026-07-27

### 新增

- 新增“首页圆角”二级设置页：支持卡片化主页列表、左右缩进、主页圆角、独立圆角卡片、会话间距、置顶会话间隙、背景颜色，以及同步到发现/通讯录/我。
- 新增“液态背景”开关：开启后所有首页圆角卡片背景跟随“液态效果”；背景颜色项目会自动置灰且不可点击。
- 主页圆角、左右缩进和发现/通讯录/我圆角均支持滑块调节，点按滑块外区域可直接输入 pt 数值。

### 优化

- 首页圆角仅处理目标列表，并缓存表格角色判断与材质状态，减少无关页面及连续列表布局的额外开销。

## [1.6.21] - 2026-07-26

### 调整

- 调整设置页顶部品牌卡：Logo 与右侧文字、版本徽标内容组分别在卡片内垂直居中。

## [1.6.20] - 2026-07-26

### 新增与优化

- 新增“聊天时间条液态”开关：时间背景随液态效果档位变化，宽度按实际文字长度自适应。
- 优化时间条渲染路径，缓存文字测量与液态材质状态，减少聊天列表布局期间的重复开销。
- 调整时间条胶囊留白，以及设置页顶部品牌卡图标的垂直对齐。
- 将“玻璃效果”设置统一命名为“液态效果”。
